<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 flex flex-col min-h-screen">
    
    <!--nav -->
    <nav class="bg-white shadow-sm border-b border-gray-100">
        <div class="max-w-6xl mx-auto px-4 py-3">
            <ul class="flex space-x-8">
                <li><a href="/" class="text-gray-600 hover:text-blue-500">Home</a></li>
                <li><a href="/profile" class="text-gray-600 hover:text-blue-500">Profile</a></li>
                <li><a href="/quiz" class="text-gray-600 hover:text-blue-500">Quiz</a></li>
                <li><a href="/auth/login" class="text-gray-600 hover:text-blue-500">Login</a></li>
                <li><a href="/auth/logout" class="text-gray-600 hover:text-blue-500">Logout</a></li>
            </ul>
        </div>
    </nav>

    <!-- main -->
    <main class="flex-grow">
        <div class="max-w-3xl mx-auto p-6 bg-white rounded-lg shadow mt-10 text-center">
          
            <div class="mb-6">
                <img 
                    src="<%= user.profile_picture %>" 
                    alt="Profile Picture" 
                    class="w-32 h-32 rounded-full mx-auto border border-gray-200 shadow-md"
                >
            </div>

            
            <h1 class="text-2xl font-bold text-gray-800">Welcome, <%= user.display_name %></h1>

            
            <h2 class="text-xl font-semibold text-gray-700 mt-2">
                Your Score: <span class="text-blue-500"><%= user.score %></span>
            </h2>

            <div class="mt-6 text-gray-600">
                <p class="mb-2">
                    <span class="font-medium">Email:</span> <%= user.email %>
                </p>
                <p class="mb-2">
                    <span class="font-medium">Account Created On:</span> <%= user.created_at %>
                </p>
            </div>

           
    </main>

    <!-- footer -->
    <footer class="bg-white shadow-sm border-t border-gray-100">
        <div class="max-w-6xl mx-auto px-4 py-4 flex justify-center items-center">
            <p class="text-gray-600 text-sm">&copy; CMPS 3390 - Project 3</p>
        </div>
    </footer>

    
    <script>
        console.log('<%= user.profile_picture %>');
    </script>

</body>
</html>
